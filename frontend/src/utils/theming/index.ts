import useBreakpoint from "./useBreakpoint";
import { shadowSwitch, colorSwitch } from "./styleSwitches";
import { palettes, breakPoints } from "./theme";

export { breakPoints, colorSwitch, palettes, shadowSwitch, useBreakpoint };
